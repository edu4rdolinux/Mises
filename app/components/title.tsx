import '../globals.scss';
import MisesText from './misesText';

const Title = () => {
    return (
        <h1 className="Title text-4xl">
            Instituto <MisesText/>
        </h1>
    );
};

export default Title;