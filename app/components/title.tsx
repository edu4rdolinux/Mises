import '../globals.scss';

const Title = () => {
    return (
        <h1 className="Title text-4xl">
            Instituto <span className="text-RedPrimary">Mises</span>
        </h1>
    );
};

export default Title;